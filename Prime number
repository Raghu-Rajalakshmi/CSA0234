#include <stdio.h>
void main()
{
int num,i,isprime=1;
printf("enter an integer");
scanf("%d",&num)
if ( num < = 1)
{
is prime =0;
}
else 
{
for (i=2; i<= num/2 ; i++)
{
if ( num %i ==0)
{
is prime = 0; 
break ;
}
if (is prime )
printf("d is prime number \n",num);
else 
printf("%d is not prime  \ n " , num );
}
