#include<stdio.h>

int main(){
    char str[100];
    int count=1;

    fgets(str,100,stdin);

    for(int i=0;str[i];i++)
        if(str[i]==' ')
            count++;

    printf("Words = %d",count);
}
