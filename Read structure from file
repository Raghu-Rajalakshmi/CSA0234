#include<stdio.h>

struct student{
    int roll;
    char name[20];
};

int main(){
    FILE *f;
    struct student s;

    f=fopen("data.txt","r");

    fread(&s,sizeof(s),1,f);

    printf("%d %s",s.roll,s.name);

    fclose(f);
}
