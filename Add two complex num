#include<stdio.h>

void reverse(char *str){
    int len=0,i;
    while(str[len]!='\0') len++;

    for(i=len-1;i>=0;i--)
        printf("%c", str[i]);
}

int main(){
    char str[100];

    printf("Enter string: ");
    scanf("%s",str);

    reverse(str);
    return 0;
}
