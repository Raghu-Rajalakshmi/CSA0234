#include<stdio.h>

struct student{
    int roll;
    char name[20];
};

int main(){
    FILE *f;
    struct student s;

    f=fopen("data.txt","w");

    scanf("%d %s",&s.roll,s.name);

    fwrite(&s,sizeof(s),1,f);

    fclose(f);
}
